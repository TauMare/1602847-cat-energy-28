div(role="group" aria-labelledby="contacts-title").line-section.-form.contacts
	h2#contacts-title._title.page__subtitle Контактные данные<br> (владельца кота)
	.contacts__container
		each input in contacts
			label.input=input.label
				input._field.-icon(type=`${input.type}` name=`${input.type}` placeholder=`${input.placeholder}` required=input.required)
				span._icon