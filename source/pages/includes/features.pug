section.features
	h2._title Как это работает
	ol._list
		each feature in features
			li._feature
				span._icon-wrapper
					svg(class=`feature__icon` width=25 height=30 aria-hidden="true" fill="currentColor")
						use(href=`img/features.svg#${feature.icon}`)
				p._text=feature.text
				//- +placeholderIcon({width: 60, className: 'features__icon', fill: "white"})
